# 品質檢查 （QA）

- 桌面視窗：`1440×900`，通過
- 手機直式視窗：`390×844`，通過
- 桌面影片：`1920×1080`、`30 fps CFR`、H.264、無音訊
- 手機影片：`1280×720`、`30 fps CFR`、H.264、無音訊
- 桌面影片容量：`20,531,086 bytes`（約 `19.6 MiB`）
- 手機影片容量：`9,380,797 bytes`（約 `8.9 MiB`）
- 桌面／手機編碼 SSIM：`0.987981` / `0.973378`，均高於 `0.90`
- 影片時長：桌面 `7.966667 秒`、手機 `7.966016 秒`
- 影片編碼：桌面 `CRF 20`、手機 `CRF 23`；兩者皆未套用銳化濾鏡
- 桌面影片載入：通過，瀏覽器 `readyState=4`
- 手機影片載入：通過，使用 `assets/vid/city-flight-m.mp4`
- 手機播放方式：與電腦版相同，影片 `currentTime` 跟隨捲動
- 影片載入方式：使用 Blob URL 掛載，不依賴主機 HTTP Range，避免背景切換後串流 seek 失效
- 載入海報：固定不位移、不縮放；影片可用後直接對齊當下捲動時間
- 手機水平取景：固定向右補償 `11.5vw`，讓電視視覺中心對齊畫面中心
- 手機水平位置：起點／中段／結尾皆使用同一固定補償，無動態左右追蹤
- `390×844` 起點／中段／結尾：影片顯示寬度恒定 `1500.44 px`
- 手機紅色電話按鈕與捲動提示：使用獨立安全間距，避免垂直重疊
- `390×844` 電話按鈕／提示間距：約 `54 px`，`overlaps=false`
- `375×667` 電話按鈕／提示間距：約 `30 px`，`overlaps=false`
- 手機文案層級間距：統一為 `20 px`
- 服務標籤：電腦版與手機版均已移除，DOM 不建立空白標籤容器
- 移除標籤後內文／CTA 間距：桌面 `28 px`、手機 `20 px`，無空白區塊
- 手機與電腦版文案退場順序：編號／眉題 → 主標題 → 說明 → 紅色電話按鈕
- 手機與電腦版背景漸層：捲動進度 `16%` 開始淡出，`58%` 完全退場
- 手機與電腦版右側紅色邊線：與漸層同步淡出，`58%` 完全消失
- 場景導覽紅點與「大型戶外電視」標籤：手機與電腦版均移除
- 漸層實測：電腦版 `40% = 0.395`、`60% = 0`；手機版 `40% = 0.391`、`60% = 0`
- 桌面初次載入：捲動 `32%` 時影片時間到達 `2.601 秒`
- 切換背景再回前景：`visibilityState` 正確由 `visible → hidden → visible`；繼續捲動到 `70%` 後影片時間到達 `5.467 秒`，`seeking=false`
- 後續訪問：同分頁重新載入後，捲動 `48%` 時影片時間到達 `3.843 秒`，`readyState=4`、`seeking=false`
- 手機捲動：`390×844` 捲動 `66%` 時使用 `city-flight-m.mp4`，影片時間到達 `5.147 秒`
- 手機影片尺寸：固定填滿視窗，捲動時不放大、不縮小
- 最後畫面：大型戶外電視置中，主要看板面積保留
- 最後畫面水平位置：維持固定右移補償
- 最後畫面文案：透明度 `0`，不覆蓋看板
- 文案位置：桌面右側
- 電話連結：`tel:0424722099`
- 靜態 SEO 文案：存在；引擎掛載後正確隱藏
- 瀏覽器錯誤與警告：`0`
- SSIM 接縫驗證：不適用；單一場景沒有相鄰接縫
- Reduced Motion／Data Saver／iOS Low Power Mode：沿用 `scrub-engine.js` 的靜態 fallback
- 直式專用影片：未生成；依使用者要求使用既有影片的手機裁切版本

## 已知限制 （Known Limitation）

- 來源為 `16:9`，直式手機無法在填滿畫面的同時保留完整橫向影像；目前使用固定右移補償進行裁切，捲動過程不再改變位置。
